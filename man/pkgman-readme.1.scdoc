PKGMAN-README(1)

# NAME

pkgman-readme - print the package's README

# SYNOPSIS

*pkgman readme* _pkgname_

# DESCRIPTION

Print the README file from a package's source directory.  The file
is optional; if absent, the command exits with an error.

If both README.md and README exist, README.md takes precedence.

See *pkgman-readme*(7) for details on format and conventions.

# ENVIRONMENT

*PAGER*
	Program used to display the README.  If unset, output is
	written to stdout.

# EXIT STATUS

*0*
	Success.
*>0*
	README not found, package not found, or error.

# SEE ALSO

*pkgman*(1),
*pkgman.conf*(5),
*pkgman-readme*(7)

; vim: ft=scdoc cc=72 tw=70
